<div class="project-contributers">

  <% @project.users.uniq.each do |user| %>

    <div class="contributer">
      <h5><%= user.name.split(" ").first %></h5>
      <%= link_to (image_tag avatar_url(user)), user_path(user)  %>
    </div>

  <% end %>

</div>