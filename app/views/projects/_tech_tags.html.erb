<div>
  <ul>
    <% @project.features.each do |feature| %>
      <% feature.technologies.each do |tech| %>
      <%= link_to image_tag(tech.icon_url, title: "#{tech.name}", rel: 'tooltip')  %>
      <% end %>
    <% end %>
  </ul>
</div>