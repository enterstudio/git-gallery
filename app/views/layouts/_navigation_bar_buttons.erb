<ul class="nav">
	<li class="active">
		<%= link_to "Homepage", current_user %>
	</li>	
	<li class="">
		<%= form_tag(users_path, :method => "get") do %>
  		<%= text_field_tag :search, params[:search], placeholder: "Search Users" %>
  	<% end %>
  </li>
</ul>

<!-- LOGIN/ACCOUNT BAR  -->
<div class="top-account-control visible-desktop">
	<!-- ACCOUNT TAB -->
	<%= link_to "#{current_user.name}", nil, class: 'top-create-account' if logged_in? %>
	<%= render 'layouts/login' %>
</div>