<p id="notice"><%= notice %></p>

<p>
  <b>Feature:</b>
  <%= @feature.title %>
</p>

<p>
  <b>Project:</b>
  <%= link_to "#{@feature.project.name}", @feature.project %>
</p>

<p>
  <b>Creator:</b>
  <%= @feature.user.name %>
</p>

<p>
  <b>Description:</b>
  <%= @feature.description %>
</p>

<p>
  <b>Technologies:</b>
  <ul>
    <% @feature.technologies.each do |technology| %>
      <li><%= link_to technology.name, user_technology_path(@feature.user, technology) %></li>
    <% end %>
  </ul>
</p>

<p>
  <b>Image(s):</b>
    <% @feature.uploads.each do |upload| %>
      <%= image_tag upload.image_url.to_s, width: '100' if upload.image? %>
    <% end %>
</p>


<%= link_to 'Edit', edit_feature_path(@feature) %> |
<%= link_to "Back to #{@feature.user.name}'s Homepage", user_features_path(@feature.user) %> | 
<%= link_to "Upload an Image", [:new, @feature, :upload] %>
<%= link_to 'Add Snippit', new_snippet_path %> |
