<h1>Gallery Members</h1>

<% @users.each do |user| %>
  <ul>
    <li><%= link_to "#{user.name}", user %><br><%= link_to (image_tag avatar_url(user)), user %></li>
    <li><%= mail_to user.email, "email" %>  
  </ul>
<% end %>

<br />

<%= link_to 'Project List', projects_path %>

